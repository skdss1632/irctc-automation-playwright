const TIMEOUTS = Object.freeze({
  VERY_SHORT: 150,
  SHORT: 200,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  MIN_PRESS_SEQ_WAIT: 50,
  MAX_PRESS_SEQ_WAIT: 90,
  MIN_TRAIN_SEARCH_WAIT: 1000,
  MAX_TRAIN_SEARCH_WAIT: 1200,
});

const Login_PAGE_TIMEOUT=Object.freeze({
  VERY_SHORT: 150,
  SHORT: 200,

});

const CAPTCHA_RETRY = Object.freeze({
  MAX_ATTEMPT: 6,
});

const VALIDATE_LOCATOR_TIMEOUT=Object.freeze({
  SIGN_IN_BTN:2000,
  DEFAULT:60000,
  HOME_PAGE:2000,
  PASSENGER_FROM:2000,
  PASSENGER_DETAILS:60000,
  INVALID_CAPTCHA:3000,
})

export { TIMEOUTS, CAPTCHA_RETRY, VALIDATE_LOCATOR_TIMEOUT };
