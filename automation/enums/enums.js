const TIMEOUTS = Object.freeze({
  VERY_SHORT: 120,
  SHORT: 135,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  MIN_PRESS_SEQ: 45,
  MAX_PRESS_SEQ: 70,
});

const RETRY = Object.freeze({
  MAX_CAPTCHA_RETRY: 8,
});

const VALIDATE_LOCATOR_TIMEOUT=Object.freeze({
  LOGIN_POPUP:1000,
  DEFAULT:60000,
  HOME_PAGE:1000,
  PASSENGER_FORM:1200,
  PASSENGER_DETAILS:60000,
  INVALID_CAPTCHA:3000,
  USER_NAME: 30000,
})

export { TIMEOUTS, RETRY, VALIDATE_LOCATOR_TIMEOUT };
