const TIMEOUTS = Object.freeze({
  VERY_SHORT: 400,
  SHORT: 600,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  EXTEND: 6000,
  MIN_CAPTCHA_WAIT: 12000,
  MAX_CAPTCHA_WAIT: 15000,
  MIN_PRESS_SEQ_WAIT: 150,
  MAX_PRESS_SEQ_WAIT: 250,
});

module.exports = { TIMEOUTS };
