const TIMEOUTS = Object.freeze({
  VERY_SHORT: 200,
  SHORT: 250,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  EXTEND: 6000,
  MIN_CAPTCHA_WAIT: 12000,
  MAX_CAPTCHA_WAIT: 15000,
  MIN_PRESS_SEQ_WAIT: 50,
  MAX_PRESS_SEQ_WAIT: 70,
  MIN_TRAIN_SEARCH_WAIT: 500,
  MAX_TRAIN_SEARCH_WAIT: 600,
});

module.exports = { TIMEOUTS };
