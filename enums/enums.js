const TIMEOUTS = Object.freeze({
  VERY_SHORT: 400,
  SHORT: 500,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  MIN_PRESS_SEQ_WAIT: 60,
  MAX_PRESS_SEQ_WAIT: 100,
  MIN_TRAIN_SEARCH_WAIT: 1000,
  MAX_TRAIN_SEARCH_WAIT: 1200,
  WAIT_FOR_ELEMENT: 60000,
});

export { TIMEOUTS };
