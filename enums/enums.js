const TIMEOUTS = Object.freeze({
  VERY_SHORT: 100,
  SHORT: 200,
  MEDIUM: 1500,
  LONG: 2000,
  VERY_LONG: 5000,
  MIN_PRESS_SEQ_WAIT: 10,
  MAX_PRESS_SEQ_WAIT: 20,
  MIN_TRAIN_SEARCH_WAIT: 900,
  MAX_TRAIN_SEARCH_WAIT: 1000,
  WAIT_FOR_ELEMENT: 60000,
});

module.exports = { TIMEOUTS };
